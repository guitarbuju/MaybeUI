import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','cdn-icons-png.flaticon.com'], // Add 'source.unsplash.com' here
  },
};

export default nextConfig;
